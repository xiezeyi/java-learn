server:
  port: 8080

spring:
  application:
    name: ms-register
# 配置 Eureka Server 注册中心
eureka:
  client:
    # 注册中心默认开启自我注册自我发现，单节点设置false,否则报错
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8080/eureka/
